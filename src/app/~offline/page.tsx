export default function OfflinePage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-6">
      <div className="max-w-sm rounded-2xl border border-zinc-800 bg-zinc-900 p-6 text-center">
        <h1 className="text-2xl font-semibold">Offline Mode</h1>
        <p className="mt-2 text-zinc-300">You can still open cached workouts and continue training.</p>
      </div>
    </main>
  );
}
